const requestButton  = document.querySelector('.request-button');

const sendRequest = () => {
// find(@project.user_id).username
  requestButton.addEventListener('click', (event) => {

    console.log("Hey I am clicked!!!!");
    console.log("I am from" <%="ruby" %>);

  });
}

export { sendRequest };
