<div class="container">
  <div class="cards">
    <% @users.each do |user| %>
    <div class="card-background">
      <%#= link_to project_path(project), class: "card" do  %>
      <a href="<%= user_path(user.username) %>" class="card">
        <%= render 'users/card', user: user %>
      </a>
    </div>
  <% end %>
  </div>
</div>

